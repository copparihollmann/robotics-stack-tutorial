CONFIG_PRINTK=y
CONFIG_MAIN_STACK_SIZE=16384
CONFIG_TIMESLICING=n
# This SoC is WithoutFPU. The FLOAT half of this benchmark is deliberate -- it is
# ModelBlaster's own reference expression, and libgcc soft-float is what it costs.
# CONFIG_FPU is not set
